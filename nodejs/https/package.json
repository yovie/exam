{
  "name": "index",
  "version": "1.0.0",
  "description": "openssl genrsa -out server-key.pem 1024 openssl req -new -key server-key.pem -out server-csr.pem openssl x509 -req -in server-csr.pem -signkey server-key.pem -out server-cert.pem",
  "main": "index.js",
  "dependencies": {
    "express": "^4.13.4",
    "fs": "0.0.2",
    "http": "0.0.0",
    "https": "^1.0.0"
  },
  "devDependencies": {},
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC"
}
